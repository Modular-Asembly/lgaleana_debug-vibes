{
  "name": "debug-vibes",
  "user": "lgaleana",
  "architecture": [
    {
      "design": {
        "type": "infrastructure",
        "name": "Cloud Run",
        "details": ""
      },
      "files": [],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "module",
        "name": "main",
        "details": "This is a FastAPI service. Use FastAPI design patterns and best practices. Place all files inside the app/ directory, including HTML templates. In app/main.py, the following steps must be executed:\n1. Call load_dotenv() immediately before any other import to load environment variables.\n2. Setup CORSMiddleware with the following hardcoded configuration:\n   - allow_origins: [\"*\"]\n   - allow_credentials: True\n   - allow_methods: [\"*\"]\n   - allow_headers: [\"*\"]\n3. Setup a security scheme for Swagger UI using FastAPI's HTTPBearer with these hardcoded values:\n   - scheme_name: \"Authorization\"\n   - description: \"Enter your Bearer token\"\n4. Include the following API routers: user_router, conversation_router, message_router, messages_all_router.\n5. Create the database tables by calling Base.metadata.create_all(bind=engine).\n\nNo component can depend on this component.",
        "env_variables": [],
        "packages": [
          "fastapi==0.115.6",
          "python-dotenv==1.0.1",
          "uvicorn==0.34.0"
        ],
        "dependencies": [
          "SQL adaptor",
          "Create or Get User",
          "Get All Users",
          "Get or Create Conversation",
          "Add Message to Conversation"
        ]
      },
      "files": [
        {
          "path": "app/main.py",
          "content": "from dotenv import load_dotenv\nload_dotenv()\n\nfrom fastapi import FastAPI\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.security import HTTPBearer\nfrom sqlalchemy.orm import Session\n\nfrom app.db import engine, Base\nfrom app.routes.user import router as user_router\nfrom app.routes.conversation_get_or_create import router as conversation_router\nfrom app.routes.message import router as message_router\nfrom app.routes.users_get_all import router as users_all_router\n\ndef create_app() -> FastAPI:\n    app = FastAPI()\n\n    # Set up CORS middleware with hardcoded configuration\n    app.add_middleware(\n        CORSMiddleware,\n        allow_origins=[\"*\"],\n        allow_credentials=True,\n        allow_methods=[\"*\"],\n        allow_headers=[\"*\"],\n    )\n\n    # Setup HTTPBearer security scheme for Swagger UI\n    app.dependency_overrides[HTTPBearer] = lambda: HTTPBearer(\n        scheme_name=\"Authorization\",\n        description=\"Enter your Bearer token\"\n    )\n\n    # Include routers\n    app.include_router(user_router)\n    app.include_router(conversation_router)\n    app.include_router(message_router)\n    app.include_router(users_all_router)\n\n    # Create database tables\n    Base.metadata.create_all(bind=engine)\n\n    return app\n\napp = create_app()\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "infrastructure",
        "name": "Cloud SQL Database",
        "details": ""
      },
      "files": [],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "module",
        "name": "SQL adaptor",
        "details": "The classic FastAPI SQL utility.\n\nConnects to a database.\nDefines Base.\nDefines a method that returns sessions. This method is used by the FastAPI endpoints.",
        "env_variables": [
          "DB_URL"
        ],
        "packages": [
          "psycopg2-binary==2.9.10",
          "sqlalchemy==2.0.36"
        ],
        "dependencies": []
      },
      "files": [
        {
          "path": "app/db.py",
          "content": "import os\nfrom typing import Generator\nfrom sqlalchemy import create_engine\nfrom sqlalchemy.orm import declarative_base, sessionmaker, Session\n\n# Get the database URL from environment variables\nDB_URL: str = os.environ[\"DB_URL\"]\n\n# Create the SQLAlchemy engine\nengine = create_engine(DB_URL, future=True)\n\n# Create a configured \"SessionLocal\" class\nSessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False, future=True)\n\n# Base class for declarative class definitions.\nBase = declarative_base()\n\ndef get_db() -> Generator[Session, None, None]:\n    \"\"\"\n    Provides a database session to be used as FastAPI dependency.\n    \n    Usage example in a FastAPI endpoint:\n\n      from fastapi import Depends\n      from app.db import get_db\n      \n      @app.get(\"/items/\")\n      def read_items(db: Session = Depends(get_db)):\n          # use db session here\n          ...\n    \"\"\"\n    db: Session = SessionLocal()\n    try:\n        yield db\n    finally:\n        db.close()\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "datamodel",
        "name": "User",
        "details": "This SQLAlchemy model represents a GitHub user. It contains an auto-generated primary key 'id', a 'github_id' field to store the GitHub ID, an email, and a 'raw_data' field to store GitHub user data as JSON.",
        "dependencies": [
          "SQL adaptor"
        ],
        "packages": [
          "sqlalchemy==2.0.36"
        ]
      },
      "files": [
        {
          "path": "app/models/user.py",
          "content": "from sqlalchemy import Column, Integer, String, JSON\nfrom app.db import Base\n\nclass User(Base):\n    __tablename__ = \"users\"\n\n    id = Column(Integer, primary_key=True, index=True)\n    github_id = Column(String, unique=True, nullable=False, index=True)\n    email = Column(String, unique=True, nullable=False, index=True)\n    raw_data = Column(JSON, nullable=False)\n\n    def __repr__(self) -> str:\n        return f\"<User(id={self.id}, github_id='{self.github_id}', email='{self.email}')>\"\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "endpoint",
        "name": "Create or Get User",
        "details": "This endpoint receives a JSON payload where the GitHub ID is provided in the 'id' field, GitHub username in the 'login' field, and email in the 'email' field. The endpoint stores the entire request payload in the field 'raw_data'. It performs the following steps:\n1. Uses a SQLAlchemy session from the SQL adaptor (dependency: SQL adaptor) to check if a User record exists with the given github_id (extracted from payload['id']).\n2. If a user exists, it returns the existing user record.\n3. If not, it creates a new User record with github_id (payload['id']), email (payload['email']), and raw_data (the entire payload), then commits the transaction and returns the newly created user.\n\nAuthentication Requirement:\n- This endpoint requires authentication via the Authentication module. The authentication dependency enforces that the caller is authenticated.\n\nHardcoded values:\n- API route prefix: '/users'\n- HTTP response status code: 200 OK\n- Extraction keys: 'id' for GitHub ID and 'email' for the user email\n\nInteracts with the User datamodel and depends on the SQL adaptor and Authentication module. Note: Although the endpoint design indicates the use of authentication, the implementation in the code snippet uses payload['sub'] for lookup, which should be reconciled with the design specification.",
        "env_variables": [],
        "packages": [],
        "dependencies": [
          "SQL adaptor",
          "Authentication",
          "User"
        ]
      },
      "files": [
        {
          "path": "app/routes/user.py",
          "content": "from fastapi import APIRouter, Depends, HTTPException, status\nfrom pydantic import BaseModel, Field\nfrom sqlalchemy.orm import Session\nfrom app.db import get_db\nfrom app.models.user import User\nfrom app.authentication import authenticate_user\nfrom typing import Any, Dict\n\nrouter = APIRouter(prefix=\"/users\", tags=[\"users\"])\n\n\nclass UserResponse(BaseModel):\n    id: int\n    github_id: str\n    email: str\n    raw_data: Dict[str, Any]\n\n    class Config:\n        orm_mode = True\n\n\n@router.post(\"/\", response_model=UserResponse, status_code=status.HTTP_200_OK)\ndef create_or_get_user(\n    payload: Dict[str, Any],\n    db: Session = Depends(get_db),\n) -> UserResponse:\n    # Check if a user with the given GitHub ID already exists.\n    existing_user: User | None = (\n        db.query(User).filter(User.github_id == payload[\"sub\"]).first()\n    )\n    if existing_user:\n        return existing_user\n\n    # Create a new user record with the entire payload as raw_data.\n    new_user = User(github_id=payload[\"sub\"], email=payload[\"email\"], raw_data=payload)\n    db.add(new_user)\n    db.commit()\n    db.refresh(new_user)\n    return new_user\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "datamodel",
        "name": "Conversation",
        "details": "This SQLAlchemy model represents a conversation. It contains an auto-generated primary key 'id', a 'github_repository' field to store the repository related to the conversation, and a foreign key 'user_id' that associates the conversation with a User. It also defines a relationship to the Message model to access all messages belonging to the conversation.",
        "dependencies": [
          "SQL adaptor"
        ],
        "packages": [
          "sqlalchemy==2.0.36"
        ]
      },
      "files": [
        {
          "path": "app/models/conversation.py",
          "content": "from sqlalchemy import Column, Integer, String, ForeignKey\nfrom sqlalchemy.orm import relationship\nfrom app.db import Base\n\nclass Conversation(Base):\n    __tablename__ = \"conversations\"\n\n    id = Column(Integer, primary_key=True, index=True)\n    github_repository = Column(String, nullable=False)\n    user_id = Column(Integer, ForeignKey(\"users.id\"), nullable=False)\n\n    # Bidirectional relationship with Message datamodel\n    messages = relationship(\"Message\", back_populates=\"conversation\", cascade=\"all, delete-orphan\")\n\n    def __repr__(self) -> str:\n        return f\"<Conversation(id={self.id}, github_repository='{self.github_repository}', user_id={self.user_id})>\"\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "datamodel",
        "name": "Message",
        "details": "This SQLAlchemy model represents a message. It contains an auto-generated primary key 'id', a 'role' field, and a 'content' field. It also includes a foreign key 'conversation_id' to associate the message with a Conversation.",
        "dependencies": [
          "SQL adaptor"
        ],
        "packages": [
          "sqlalchemy==2.0.36"
        ]
      },
      "files": [
        {
          "path": "app/models/message.py",
          "content": "from sqlalchemy import Column, Integer, String, ForeignKey, Text\nfrom sqlalchemy.orm import relationship\nfrom app.db import Base\n\nclass Message(Base):\n    __tablename__ = \"messages\"\n\n    id = Column(Integer, primary_key=True, index=True)\n    role = Column(String, nullable=False)\n    content = Column(Text, nullable=False)\n    conversation_id = Column(Integer, ForeignKey(\"conversations.id\"), nullable=False)\n\n    # Bidirectional relationship with Conversation\n    conversation = relationship(\"Conversation\", back_populates=\"messages\")\n\n    def __repr__(self) -> str:\n        return f\"<Message(id={self.id}, role='{self.role}', conversation_id={self.conversation_id})>\"\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "endpoint",
        "name": "Add Message to Conversation",
        "details": "This endpoint adds a new message to an existing conversation. It expects a JSON body containing 'conversation_id' and 'content'. It validates that the conversation exists using the Conversation datamodel, then creates two new Message records associated with that conversation:\n1. A user message is created with a hardcoded role of 'user' and its content taken from payload.content.\n2. An assistant message is created with a hardcoded role of 'assistant' and content 'Test'.\nAfter adding both messages, the endpoint commits the transaction, refreshes the assistant message, and returns the assistant message as the response.\n\nAuthentication Requirement:\n- This endpoint now requires authentication via the Authentication module. The authentication dependency enforces that the caller is authenticated.\n\nHardcoded values:\n- API route prefix: '/messages'\n- HTTP response status code: 201 Created\n- Roles: User message role is hardcoded as 'user'; assistant message role is hardcoded as 'assistant'\n- Assistant message content is hardcoded as 'Test'\n- Conversation validation: Uses Conversation datamodel to ensure the conversation exists\n\nInteracts with Message and Conversation datamodels and depends on SQL adaptor and Authentication module.",
        "env_variables": [],
        "packages": [],
        "dependencies": [
          "Message",
          "SQL adaptor",
          "Conversation",
          "Authentication"
        ]
      },
      "files": [
        {
          "path": "app/routes/message.py",
          "content": "from fastapi import APIRouter, Depends, HTTPException, status, Request\nfrom pydantic import BaseModel, Field\nfrom sqlalchemy.orm import Session\nfrom typing import Any, Dict\n\nfrom app.db import get_db\nfrom app.models.conversation import Conversation\nfrom app.models.message import Message\nfrom app.authentication import authenticate_user\n\nrouter = APIRouter(prefix=\"/messages\", tags=[\"messages\"])\n\nclass UserMessageRequest(BaseModel):\n    conversation_id: int = Field(..., description=\"ID of the conversation\")\n    content: str = Field(..., description=\"Content of the message\")\n\nclass MessageResponse(BaseModel):\n    id: int\n    role: str\n    content: str\n    conversation_id: int\n\n    class Config:\n        orm_mode = True\n\n@router.post(\n    \"/\",\n    response_model=MessageResponse,\n    status_code=status.HTTP_201_CREATED,\n)\ndef add_message(\n    payload: UserMessageRequest,\n    db: Session = Depends(get_db),\n    current_user: Any = Depends(authenticate_user)\n) -> MessageResponse:\n    # Validate that the conversation exists\n    conversation = db.query(Conversation).filter(Conversation.id == payload.conversation_id).first()\n    if not conversation:\n        raise HTTPException(\n            status_code=status.HTTP_404_NOT_FOUND,\n            detail=f\"Conversation with id {payload.conversation_id} not found\",\n        )\n    \n    # Create new Message record\n    user_message = Message(\n        role=\"user\",\n        content=payload.content,\n        conversation_id=payload.conversation_id\n    )\n    db.add(user_message)\n    ai_message = Message(\n        role=\"assistant\",\n        content=\"Test\",\n        conversation_id=payload.conversation_id\n    )\n    db.add(ai_message)\n    db.commit()\n\n    db.refresh(ai_message)\n    return ai_message\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "module",
        "name": "Authentication",
        "details": "This module provides an authentication method to be used as a FastAPI dependency. The primary method, authenticate_user, uses FastAPI's HTTPBearer security scheme to extract a GitHub access token from the request headers. It then calls the GitHub API (https://api.github.com/user) with a hardcoded header format ('Authorization': 'token {token}') to fetch the user's public data. The response is cached in an in-memory dictionary to avoid redundant calls for the same token. Important extracted hardcoded values include:\n- HTTPBearer initialization: scheme_name='Authorization', description='Enter your Bearer token'\n- GitHub API endpoint: 'https://api.github.com/user'\n- Authorization header format: 'token {token}'\n- Convert GitHub 'id' to string for User lookup\n\nAfter fetching the GitHub user data, the method extracts the GitHub ID using the key 'id' from the JSON response and uses it to query the User datamodel via SQLAlchemy (dependency: SQL adaptor). If the user is not found or if the credentials are invalid/missing, appropriate HTTP exceptions with corresponding status codes (401 for unauthorized, 404 for not found) are raised.",
        "env_variables": [],
        "packages": [
          "requests==2.31.0"
        ],
        "dependencies": [
          "SQL adaptor",
          "User"
        ]
      },
      "files": [
        {
          "path": "app/authentication.py",
          "content": "import os\nfrom typing import Any, Dict\n\nimport requests\nfrom fastapi import Depends, HTTPException, Request, status\nfrom fastapi.security import HTTPBearer, HTTPAuthorizationCredentials\nfrom sqlalchemy.orm import Session\n\nfrom app.db import get_db\nfrom app.models.user import User\n\n# A simple in-memory cache to store GitHub API responses\n_auth_cache: Dict[str, Dict[str, Any]] = {}\n\n# Initialize the security scheme\nsecurity = HTTPBearer(\n    scheme_name=\"Authorization\",\n    description=\"Enter your Bearer token\"\n)\n\ndef fetch_github_user(token: str) -> Dict[str, Any]:\n    \"\"\"\n    Calls the GitHub API to fetch public user data using the provided access token.\n    Caches the result to avoid unnecessary API calls.\n\n    Args:\n        token (str): GitHub access token.\n\n    Returns:\n        Dict[str, Any]: User data from GitHub.\n    \"\"\"\n    if token in _auth_cache:\n        return _auth_cache[token]\n\n    headers = {\"Authorization\": f\"token {token}\"}\n    response = requests.get(\"https://api.github.com/user\", headers=headers)\n    if response.status_code != 200:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Invalid GitHub token or unable to fetch user data\",\n        )\n    user_data = response.json()\n\n    # Cache the result\n    _auth_cache[token] = user_data\n    return user_data\n\n\ndef authenticate_user(\n    credentials: HTTPAuthorizationCredentials = Depends(security),\n    db: Session = Depends(get_db)\n) -> User:\n    \"\"\"\n    FastAPI dependency to authenticate a user using a GitHub access token.\n    Uses HTTPBearer security scheme to extract the token, calls the GitHub API to get user data,\n    and retrieves the corresponding User record from the database using the GitHub ID.\n    \n    Caching is used to minimize GitHub API calls for identical tokens.\n    \n    Args:\n        credentials (HTTPAuthorizationCredentials): The bearer token credentials.\n        db (Session): SQLAlchemy session dependency.\n    \n    Returns:\n        User: The authenticated User.\n    \n    Raises:\n        HTTPException: If token is missing, invalid, or user is not found in the database.\n    \"\"\"\n    if not credentials:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Authorization credentials missing\",\n        )\n\n    token = credentials.credentials\n    github_user = fetch_github_user(token)\n\n    # Extract GitHub ID from response\n    github_id = str(github_user[\"id\"])  # Convert to string since our model stores it as String\n    if not github_id:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"GitHub ID not found in response\",\n        )\n\n    # Retrieve the corresponding User record from the database using github_id\n    user = db.query(User).filter(User.github_id == github_id).first()\n    if not user:\n        raise HTTPException(\n            status_code=status.HTTP_404_NOT_FOUND,\n            detail=\"User not found in the system\",\n        )\n\n    return user\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "endpoint",
        "name": "Get or Create Conversation",
        "details": "This endpoint handles two use cases based on the provided 'github_repository'. It first authenticates the user via the Authentication module. It accepts a query parameter 'github_repository' as input. The endpoint then checks if a Conversation exists for the authenticated user and the specified repository. If it doesn't exist, a new Conversation is created with no messages. Regardless, the endpoint returns the conversation along with all its associated messages. The response is constructed using a ConversationResponse schema that includes the conversation's ID, github_repository, user_id, and a list of messages formatted using MessageResponse. Each MessageResponse includes the id, role, content, and conversation_id.\n\nAuthentication Requirement:\n- The endpoint requires authentication via the Authentication module (dependency enforced using authenticate_user).\n\nHardcoded values:\n- API route prefix: '/conversations'\n- HTTP response status code: 200 OK\n- Input parameter: 'github_repository' is a required query parameter\n- ORM: Uses from_orm method to construct response for messages\n\nInteracts with Message and Conversation datamodels and depends on SQL adaptor and Authentication module.",
        "env_variables": [],
        "packages": [],
        "dependencies": [
          "Conversation",
          "SQL adaptor",
          "Authentication",
          "Message"
        ]
      },
      "files": [
        {
          "path": "app/routes/conversation_get_or_create.py",
          "content": "from typing import List\n\nfrom fastapi import APIRouter, Depends, HTTPException, Query, status\nfrom pydantic import BaseModel, Field\nfrom sqlalchemy.orm import Session\n\nfrom app.authentication import authenticate_user\nfrom app.db import get_db\nfrom app.models.conversation import Conversation\nfrom app.models.message import Message\n\nrouter = APIRouter(prefix=\"/conversations\", tags=[\"conversations\"])\n\n\nclass MessageResponse(BaseModel):\n    id: int = Field(..., description=\"Message identifier\")\n    role: str = Field(..., description=\"Role of the message sender\")\n    content: str = Field(..., description=\"Content of the message\")\n    conversation_id: int = Field(..., description=\"Associated conversation ID\")\n\n    class Config:\n        orm_mode = True\n        from_attributes=True\n\n\nclass ConversationResponse(BaseModel):\n    id: int = Field(..., description=\"Conversation identifier\")\n    github_repository: str = Field(\n        ..., description=\"GitHub repository associated with the conversation\"\n    )\n    user_id: int = Field(..., description=\"ID of the user owning this conversation\")\n    messages: List[MessageResponse] = Field(\n        default_factory=list, description=\"List of messages in the conversation\"\n    )\n\n    class Config:\n        orm_mode = True\n\n\n@router.get(\"/\", response_model=ConversationResponse, status_code=status.HTTP_200_OK)\ndef get_or_create_conversation(\n    github_repository: str = Query(..., description=\"GitHub repository identifier\"),\n    db: Session = Depends(get_db),\n    current_user=Depends(authenticate_user),\n) -> ConversationResponse:\n    \"\"\"\n    Get or create a conversation based on the provided GitHub repository.\n    Authenticates the user via the authentication module. Then, it looks for a conversation record\n    that belongs to the authenticated user and matches the given GitHub repository. If it doesn't exist,\n    a new conversation is created. Regardless, it returns the conversation along with all its associated messages.\n    \"\"\"\n    # Look for an existing conversation for current user and given repository.\n    conversation: Conversation | None = (\n        db.query(Conversation)\n        .filter(Conversation.user_id == current_user.id)\n        .filter(Conversation.github_repository == github_repository)\n        .first()\n    )\n\n    if not conversation:\n        # Create new conversation record with no messages.\n        conversation = Conversation(\n            github_repository=github_repository, user_id=current_user.id\n        )\n        db.add(conversation)\n        db.commit()\n        db.refresh(conversation)\n\n    # Load associated messages (relationship should be lazy loaded)\n    messages = conversation.messages\n\n    return ConversationResponse(\n        id=conversation.id,\n        github_repository=conversation.github_repository,\n        user_id=conversation.user_id,\n        messages=[MessageResponse.from_orm(msg) for msg in messages],\n    )\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    },
    {
      "design": {
        "type": "endpoint",
        "name": "Get All Users",
        "details": "This endpoint retrieves all user records from the database. It authenticates the request using the Authentication module to ensure that only authenticated requests are served. It then uses a SQLAlchemy session from the SQL adaptor to query and return all records from the User datamodel.",
        "env_variables": [],
        "packages": [],
        "dependencies": [
          "SQL adaptor",
          "Authentication",
          "User"
        ]
      },
      "files": [
        {
          "path": "app/routes/users_get_all.py",
          "content": "from typing import List\n\nfrom fastapi import APIRouter, Depends, status\nfrom sqlalchemy.orm import Session\n\nfrom app.db import get_db\nfrom app.models.user import User\nfrom app.authentication import authenticate_user\nfrom pydantic import BaseModel\n\nrouter = APIRouter(\n    prefix=\"/users\",\n    tags=[\"users\"]\n)\n\nclass UserResponse(BaseModel):\n    id: int\n    github_id: str\n    email: str\n    raw_data: dict\n\n    class Config:\n        orm_mode = True\n\n@router.get(\n    \"/\",\n    response_model=List[UserResponse],\n    status_code=status.HTTP_200_OK,\n    summary=\"Retrieve all users\",\n    description=\"Retrieve all user records from the database. Requires authentication.\"\n)\ndef get_all_users(\n    db: Session = Depends(get_db),\n    current_user: User = Depends(authenticate_user)\n) -> List[UserResponse]:\n    users: List[User] = db.query(User).all()\n    return users\n"
        }
      ],
      "update_status": "up_to_date",
      "is_deployed": false
    }
  ],
  "github": "https://github.com/Modular-Asembly/lgaleana_debug-vibes"
}